2.2:
  Rework the wifi examples.
    Split Evt and Poll modules for wifi
    Significantly expanded Poll module
  Some style changes in Memory module

2.1.3.1:
  Change library paths for dlopen so they work with gentoo (are fixed?)

2.1.3:
  Add option to change section divider in Dzen2, Ascii, Utf8 and Guess out.
  Fix a color bug in i3bar output
  make the helper redirec ghc output to stderr, so i3bar doesn't break

2.1.2:
  Add the startLoopT with a tick timer
  Revamp event system to trigger a new line print
  make the thermal_zone guesser better with old kernels
  update Pulse code for new library version
  Add output modifier

2.1.0:
  Add pulseaudio support and deprecate old Alsa Example location
  Fix Battery colours (only <20 worked earlier)
  Add changeImage to Monky.Examples.Modify
  Dzen2 Output:
    Automatically add '/' to the end of xbm path
    Add getDzenOut' to use ./xbm/ for xbm path
  Fix Guess output i3bar detection
  Couple of documentation changes and refactors
  Wifi collector:
    Fix PollModule instance
    Fix signal strength/quality handling

2.0.1:
  Add license headers where missing
  Fix battery module padding
  Fix memory handle for old kernels and add getMemoryFreeHandle
  Add modify example
  Add IP collectors (no example though)
  Add guess output, to guess correct output
  Add I3 output for i3-bar
  some internal refactors
